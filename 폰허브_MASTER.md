# ★ 폰 허브 MASTER — 통합본

**오늘 나는 죽인다, 나를** · 서울 프린지 2026 · 8/15·16·18 · 미진플로어 B1
**작성 2026-08-10 (D-5) · 설치 8/14 (D-4)**

> **위상: 폰 허브에 관해 열어야 할 곳은 여기 하나다.**
> `21`(명세) · `22`(측정 설계) · `30`(안내판·NFC 실물)의 내용을 여기로 합치고,
> **어제 구축한 실제 코드와 대조해 "설계에만 있던 것"과 "정말 만들어진 것"을 구분해 표시했다.**
> 원본 3개는 삭제하지 않고 그대로 둔다 — **경위·근거가 필요할 때만** 연다.
> 🔴 앞으로 폰 허브에 관한 결정은 **이 문서를 최신화**하고 `11_디벨롭로그.md`에 로그를 남긴다.
>
> 배포 명령어·단계별 실무 절차는 같은 폴더의 **`README.md`** (GitHub에 함께 올라가는 파일).

---

## 목차

1. [30초 요약](#1-30초-요약)
2. [세 층의 관계 — 폰허브·태깅·자동감지](#2-세-층의-관계--폰허브태깅자동감지)
3. [전체 구조와 귀속 메커니즘](#3-전체-구조와-귀속-메커니즘)
4. [기능 명세 — 구현 여부 대조표](#4-기능-명세--구현-여부-대조표)
5. [화면 흐름 (실제 구현본)](#5-화면-흐름-실제-구현본)
6. [데이터 스키마](#6-데이터-스키마)
7. [측정 — 무엇이 남는가](#7-측정--무엇이-남는가)
8. [파생 지표와 산출 방법](#8-파생-지표와-산출-방법)
9. [인프라·배포](#9-인프라배포)
10. [TouchDesigner 연동](#10-touchdesigner-연동)
11. [QR·NFC 실물 사양](#11-qrnfc-실물-사양)
12. [폴백 — 폰이 죽어도 전시는 성립한다](#12-폴백--폰이-죽어도-전시는-성립한다)
13. [회수·백업](#13-회수백업)
14. [🔴 컨펌 대기](#14--컨펌-대기)
15. [이번에 뺀 것 · 추후 논의](#15-이번에-뺀-것--추후-논의)
16. [파일 목록](#16-파일-목록)

---

## 1. 30초 요약

- **폰 허브 = 관객이 자기 폰 브라우저로 여는 웹앱.** 앱 설치 없음. QR·NFC로 입장.
- **★ 1번 목적: 관객 경험의 깊이를 더하고 작품을 온전히 경험하게 하는 것.**
  측정은 그 목적을 확인·개선하기 위한 수단이지 목적이 아니다.
- **분류 원칙:** "작품용/데이터용" 이분법을 쓰지 않는다.
  **모든 기능은 ①경험 목적을 갖고 ②동시에 데이터를 남긴다.**
- **작품 유형에 따라 역할이 달라진다:**
  인터랙티브 작품(메타로즈)에서는 맥락·기록 담당 /
  **인터랙티브가 아닌 작품에서는 폰 허브가 작품과 교류하게 하는 인터랙티브 모듈 자체가 된다** — 사업 확장 시 주 시장.
- **데이터 수요자 3종:** 기관 / 관객 / 아티스트 — **raw는 하나, 가공만 셋.**
- **🔴 절대 원칙: 폰이 죽어도 전시는 성립해야 한다(graceful degradation).**
  폰은 보조, 물리 인터랙션이 본체.

### 현재 상태 (2026-08-10)

| | 상태 |
|---|---|
| 관객 웹앱 | 🟢 **구축 완료.** 여정 전체 동작. 자동 테스트 통과(여정 14 / 회복력 12 / TD 브리지 14) |
| 배포 | ⬜ **GitHub Pages 켜기만 하면 됨** — 🔴 최상위 블로커 |
| Supabase | ⬜ 스키마 SQL 준비됨. 키만 넣으면 연결. **비어 있어도 앱은 전부 작동** |
| TD 연동 | 🟢 브리지 완성·실측 통과. ⬜ 각 TD 프로젝트에 붙이는 작업 남음 |
| QR·NFC | ⬜ URL 확정 즉시 생성 가능 (스크립트 준비됨) |
| 문구·팔레트 | 🟡 전부 초안. 작가 컨펌 대기 |

> **⚠️ 상태 정정 (8/10):** `10 MASTER`(8/7)의 *"폰 허브 잔여는 3건뿐"*은 과소평가였다.
> S11-D~H로 만들어진 것은 **부1 전용 로컬 서버 + phone_test.html**(TD 내부, 별도 파일 없음)이고
> **관객이 실제로 쓰는 여정은 존재하지 않았다.** 8/10에 새로 구축했다.

---

## 2. 세 층의 관계 — 폰허브·태깅·자동감지

| | 무엇인가 | 무엇을 만들어내나 | 상태 |
|---|---|---|---|
| **폰 허브** | 관객의 웹앱 | **신원(세션)** — 이 사람이 누구인지 | 🟢 구현됨 |
| **태깅** | 관객의 의도적 동작(QR·NFC) | **결합** — 이 세션이 지금 이 모듈에 있다 | 🟢 구현됨 |
| **자동감지** | 관객 무동작 센서 | **관측** — 다가왔다·머물렀다·떠났다 | ⬜ 이번엔 안 함 |

**왜 셋이 다 필요한가**

- 폰 허브가 없으면 나머지가 무의미 — **"누구인지"가 폰에서 발급되는 세션에서만 나온다.** 세션ID가 모든 데이터의 열쇠.
- 태깅이 없으면 인터랙션 기록에 **주인이 없다.**
- 자동감지가 없으면 **"인터랙션 없이 접근했다 그냥 간" 행동이 통째로 사라진다.**

→ 대체재가 아니라 서로 다른 층.

**핵심 원리 2개**

1. **신원은 폰에서, 위치는 태깅·감지에서.** 둘이 만나야 "누가 어디에"가 된다.
2. **익명 자동감지는 개인 데이터와 절대 합쳐지지 않는다** → 분석 시 두 층이 따로 나온다(개인 여정 / 공간 통계).
   그래서 `anon_presence` 테이블에는 **`session_id` 컬럼이 없다. 그게 설계다.**

---

## 3. 전체 구조와 귀속 메커니즘

```
[관객 폰 브라우저]  ←HTTPS→  [GitHub Pages(정적) + Supabase(DB)]
                                    ↕ REST
        [메인1 PC · TD]  [부1 PC · TD]  [부2 PC · TD]
                 └ 각자 로컬 CSV에도 동시 기록(보험)

[QR·NFC 스티커] = 전기·네트워크 없는 URL 이정표. 서버와 직접 연결 없음.
```

### 귀속(attribution)은 서버가 시각으로 맞춘다

```
14:22:00  관객이 QR/NFC로 진입 → station_presence: session_A가 station_01에 있음
14:23:05  TD → events: "station_01, 장미7, 홀드 3초"   (TD는 누구인지 모름)
          브리지가 v_active_at_station을 보고 session_A를 붙여서 보냄
```

**TD는 사람을 식별하지 않는다. 못 한다.** TD가 하는 일은 두 가지뿐:

- **보내기:** 인터랙션 로그 + 타임스탬프
- **받기:** 개인화 데이터(색·명명) 구독

### ★ 개인 귀속 vs 팀 귀속 — 반드시 이해할 것

관객은 **2~4인 팀으로 함께 이동**한다. 메인1은 **인간사슬(물+손잡기+장미터치)로 회로를 물리적으로 공유**하므로,
TD는 "장미 7번이 눌렸다"는 알아도 **팀 중 누구 손인지 물리적으로 알 수 없다.** 어떤 감지 기술로도 해결 불가.

| 대상 | 귀속 | 근거 |
|---|---|---|
| 메인1 장미 터치·홀드·조합 | **팀** | 인간사슬 = 구조적으로 항상 2인+ |
| 부1 게임 플레이 | **팀** | 한 컨트롤러 함께 사용 |
| 부1 엔딩 저장 | 개인 | 각자 폰 |
| **부2 발견·엔딩** | **개인 가능 ★** | **색 시그니처가 식별자로 작동** — 한 화면에 2명이 서로 다른 슬롯, 슬롯 색으로 확정 |
| 폰 입력 전부(가명·명명·색·설문·캡처) | 개인 | 본인 입력 |

→ `events.scope`로 구분(`'individual' | 'team' | 'anonymous'`).
분석 시 메인1 조합 변화는 **팀 지표**, 가명→최종명명 변화는 **개인 지표**로 읽어야 함.

---

## 4. 기능 명세 — 구현 여부 대조표

> 🟢 구현됨 / 🟡 구현됐으나 문구·값이 초안 / ⬜ 이번에 뺌

### A. 작품 레이어 (관객 경험의 일부)

| # | 기능 | 내용 | 상태 |
|---|---|---|---|
| A1 | QR 입장·세션 발급 | 계정 불필요. 토큰을 localStorage 저장. **세션 id를 클라이언트에서 만들어 오프라인에서도 발급된다** | 🟢 |
| A2 | 인트로 | 양가성 + "장미=싫어하는 나/좋아하는 나" 자기연결. 압축 기본 + [더 읽기] 상세 레이어 | 🟡 문구 초안 |
| A3 | 색 선택 | 개인별 1색. **부2 아바타 시그니처 → 개인 식별자 역할.** 8~12색 팔레트, 자유 색상환 금지 | 🟡 팔레트 초안 |
| A4 | **가명(임시 명명)** | 대기 중 간단 질문으로 생성. **🔒 고정 문구: "가명(假名) — 진짜 이름은 안에서, 손으로 짓게 됩니다"** | 🟡 |
| A5 | 모듈 진입 | QR/NFC 탭 → "시작할까요?" → 리마인드+인스트럭션. URL `?station=NN` | 🟢 |
| A6 | **최종 명명(2박자)** | ①미워했던 면 → ②"그 '나'의 다른 면은?" → 한 문장. **가명을 덮어씀** | 🟢 ⚠️아래 참조 |
| A7 | 캡처 | 폰 버튼 → 서버 → TD가 현재 프레임 저장 → 폰에 반환. 재캡처·캐러셀 | 🟢 |
| A8 | 부1 엔딩 저장 | TD 프레임 → 세션 귀속 → 폰 | 🟢 |
| A9 | 부2 스크린샷 | 팀 내 2명 순차, 각자 저장 | 🟢 |
| A10 | 말 남기기 | 동반자에게 메시지 | ⬜ 컷 |
| A11 | 설문 | **최소 한 줄 필수. ★마무리 행동** | 🟡 문항 초안 |
| A12 | 결과 화면 | **스크린샷 그대로 전달**(합성 안 함). 극성 비율. 여정 목록 | 🟢 |
| A13 | 인스타 공유 | Web Share API | 🟢 (지원 기기만) |
| A14 | 기념품·인스타 QR | 출구 | 🟡 URL 미입력 |

> **⚠️ A6 설계 변경 (8/10) — 2박자 자동 합치기를 폐기했다.**
> `{a}지만 {b} 나` 템플릿이 "우울한 나" + "침착했지"에서
> **"우울한 나지만 침착했지 나"**라는 말이 안 되는 문장을 만들었다. **한국어 어미 변화는 규칙으로 못 푼다.**
> → 두 박자를 그대로 보여주고 **마지막 한 줄은 관객이 직접 쓴다.**
> 이어붙인 초안(`{a}, {b}`)을 넣어주되 관객이 고치고, 손봤는지 여부(`edited_seed`)도 기록한다.
> `10 MASTER §논문원칙`의 *"감정 어휘를 시스템이 정하지 않는다 · 어휘는 관객 것"*과도 이쪽이 맞다.

### B. 큐레이토리얼 — 경험의 깊이를 만드는 장치

> **"운영·안내"가 아니다.** 관객이 작품을 온전히 경험하게 하는 것이 1번 목적이고,
> 이 텍스트들이 그 역할을 한다. **그리고 읽기 행동 자체가 데이터가 된다.**

| # | 기능 | 경험 목적 | 상태 |
|---|---|---|---|
| B1 | 스테이션별 안내 | 자기연결 리마인드 + 모듈 고유 인스트럭션(회로 완성법 등) — **이걸 못 읽으면 인터랙션 자체가 성립 안 함** | 🟡 |
| B2 | 인트로 상세 레이어 | 더 알고 싶은 관객에게 깊이 제공 | 🟡 |
| B3 | 라이브 혼잡도 | 기다림의 불안을 줄여 경험의 리듬 유지 | 🟢 |
| B4 | 유료/무료 경계 안내 | 기대 관리 | ⬜ 미작성 |
| **B5** | **부1 규칙 전체 설명 (opt-in)** | **막혔을 때의 안전망** | 🟢 |

**B5 상세 — 🔴 이 조건들이 코드에 박혀 있고 테스트로 고정돼 있다**

- **범위: 전부.** 아이템 4종의 성격 + **엔딩 조건까지** 설명한다
- **⚠️ 반드시 opt-in.** 기본 화면에 띄우지 않는다. *"모르겠으면 여기"*를 **관객이 직접 눌러야** 열린다
- **왜 opt-in이어야 하나:** 이 작품의 엔진은 **"규칙 발견"**이다(`23 §3-3-1`).
  기본 노출하면 발견이 사라지고, 한 명이 보고 팀에 알려주면 **팀 전체가 그 경험을 잃는다**
- **📊 열람 자체가 지표다.** *"몇 명이 막혀서 결국 봤는가"* = 난이도·전달 실패 측정
- 🔴 **규칙 조회 시간은 몰입 지표(C6)에서 분리 집계할 것** →
  `rules_close` 이벤트에 **`exclude_from_immersion: true`** 표식이 자동으로 붙는다

### C. 데이터 수집 — 모든 기능에서 동시에 발생

| # | 기능 | 내용 | 상태 |
|---|---|---|---|
| C1 | 이벤트 로깅 | 모든 행동을 `events`에. session·team·station·type·시각·scope·seq·payload | 🟢 |
| C2 | TD 로그 수집 | 각 TD가 인터랙션 상세를 전송(+로컬 CSV 동시 기록) | 🟢 |
| C3 | **세션 종료 마킹** | 🔴 **스크린샷과 분리.** 부1은 **엔딩 도달 시점**이 종료 이벤트(60초 안에 촬영을 안 하고 끝나는 경우가 있으므로). 설문 완료도 종료 신호. 미이행자는 **회차 종료 자동 마감** | 🟢 |
| C4 | 오프라인 큐잉 | 끊기면 브라우저에 쌓고 복구 시 전송. **⚠️ 타임스탬프는 '행동한 순간'** | 🟢 |
| C5 | 읽기 행동 측정 | 체류·스크롤 깊이·완독·[더 읽기]·재열람·읽기속도 → **이해 실패 지점 탐지** | 🟢 |
| C6 | 폰 미사용 시간 | 화면 꺼짐/앱 전환 → **폰을 주머니에 넣고 작품을 본 시간 = 몰입 지표** | 🟢 |
| C7 | 입력 과정 측정 | 선택까지의 시간·수정 횟수·지운 횟수 → 망설임·언어 찾기의 어려움 | 🟢 |
| C8 | 익명 재실 통계 | 자동감지(익명형). 개인 데이터와 **결합 불가** | ⬜ 컷 (테이블만 준비) |
| C9 | **규칙 조회 이벤트** | B5를 **연 시점·머문 시간**. **몰입 지표에서 분리 집계** | 🟢 |

---

## 5. 화면 흐름 (실제 구현본)

```
[입구 — QR station=00]
 1. 동의 (+ 상세 레이어: 무엇을 모으나·보관기간·삭제 방법)
 2. 인트로 (압축 + [더 읽기])
 3. 색 선택
 4. 가명 생성  ← 🔒 "가명 — 진짜 이름은 안에서, 손으로" 고정 노출
 5. 대기 화면 (혼잡도 + 진입 안내)

[여정 — 각 안내판의 QR·NFC. 부1↔부2 순서 자유]
 6. 탭 → "시작할까요?" → 리마인드 + 인스트럭션
 7. 01 메인1: 최종 명명 2박자 → 캡처·캐러셀·재캡처
 8. 02 부1:  엔딩 저장 / [모르겠으면 여기] = B5 opt-in 규칙
 9. 03 부2:  스크린샷 (팀 내 2명 순차)
10. 04 기록: 안내만

[출구 — QR station=05]
11. 설문 (자유서술 1줄 ★필수 + 척도 4 + 선택 1)
12. 결과 화면 = 설문 완료 보상 (이름·색·극성 비율·스크린샷·여정)
13. 기념품·인스타
```

**⭐ 중간 QR로 먼저 들어와도 안전하다.** 세션이 없으면 입구 흐름을 먼저 태우고,
끝나면 **원래 찍었던 방으로 자동 복귀**한다(`pendingStation`). 테스트로 고정돼 있다.

---

## 6. 데이터 스키마

> 실물 = `supabase_schema.sql`. **`events.payload`가 JSONB라 작품이 바뀌어도 스키마가 불변**이고,
> 이 이식성이 곧 재사용 자산이다.

```
teams            : id, round_no, member_count, formed_at
sessions         : id, team_id, status, entered_at, exited_at, end_reason,
                   round_no, ticket_type, consent, ab_group,
                   color, color_name, pseudonym, final_name, final_name_a/b,
                   lang, device, schema_version
station_presence : id, session_id, station_id, entered_at, left_at, via
events           : id, session_id, team_id, station_id, event_type, scope,
                   occurred_at, created_at, seq, source, payload, schema_version
artifacts        : id, session_id, station_id, type, value,
                   image_path, image_url, meta, occurred_at
survey           : id, session_id, question_id, answer, answer_num, meta, occurred_at
anon_presence    : id, station_id, sensor, occupied, head_count, occurred_at
                   ★ session_id 없음 — 개인 결합 불가가 설계다
```

### 🔴 harvest 원칙 8개 — 구현 시 반드시 지킬 것

| 원칙 | 이유 | 구현 |
|---|---|---|
| **타임스탬프는 "행동한 순간"으로 기록** | 전송 시각으로 찍으면 체류·순서 데이터가 전부 오염됨 | `occurred_at` / `created_at` 분리 ✅ |
| 모든 이벤트에 session·team·station·scope | 나중에 어떤 축으로도 재집계 가능 | ✅ |
| payload는 JSON으로 유연하게 | 작품이 바뀌어도 스키마 불변 = 이식성 | ✅ |
| 이벤트에 순번(sequence) 부여 | 유실 감지 가능 | `seq` + 유니크 인덱스 ✅ |
| 🔴 **세션 종료 마킹은 스크린샷과 분리** | 부1 촬영이 엔딩 60초 안에서만 가능해지고, 안 찍고 자동 종료되는 경우가 생김 | `ending_reached` ✅ |
| 세션 종료 폴백 | 설문 미이행자는 회차 종료 시각으로 자동 마감 | `close_stale_sessions()` ✅ |
| 회차 종료마다 카운트 대조 | 클라우드 vs 로컬 건수 비교 → 유실 즉시 발견 | `queue_size()` + CSV ✅ |
| 스키마 버전 필드 | 전시마다 달라져도 과거 데이터 해석 가능 | `schema_version` ✅ |

> **⚠️ 8/10 발견·수정:** 동의 화면에서 **세션 발급 전에 쌓인 이벤트가 고아가 되고 seq가 겹쳤다.**
> → 세션 발급 시 **미귀속 이벤트를 소급 귀속**(backfill). 이 구간이 §7-1의
> *"맥락을 얼마나 받아들였나"*가 나오는 자리라 버리면 안 된다.

---

## 7. 측정 — 무엇이 남는가

> 원칙: **"무엇을 측정할까"를 정하기 전에, 큰 추가 노력 없이 뽑아낼 수 있는 raw data를 남김없이 모은다.**
> 무엇을 쓸지·어떻게 가공할지는 나중에 정한다. **raw는 하나, 가공만 셋.**

### 7-0. 수요자 3종 — 같은 raw, 다른 가공

| 대상 | 무엇을 궁금해하나 | 가공 방향 |
|---|---|---|
| **기관·미술관** | 성과 보고·예산 정당화·다음 기획 판단 | 총계·비율·완주율·체류·벤치마크 |
| **관객 개인** | 나는 어떤 경험을 했나 | 나의 여정 시각화·나의 명명·전체 대비 내 위치 |
| **아티스트** | 내 작품이 의도대로 작동했나 | 어디서 막혔나·반응이 컸던 요소·이해 실패 지점 |

### 7-1. 폰에서 나오는 것 🟢 전부 구현

**읽기 행동** — 체류 시간 / 스크롤 깊이(25% 단위) / 하단 도달=완독 / [더 읽기] 열람 /
읽기 속도(글자수÷초, 정독 vs 훑기) / **되돌아와 다시 읽었는가·몇 번**(★ 이해 실패 지점 탐지) / 언어 선택

**폰을 안 본 시간 (몰입)** — 화면 꺼짐·앱 전환 구간 / 모듈별 미사용 시간
→ 이 지표는 **폰 허브가 자기 사용량을 줄이는 것을 성공으로 측정**한다는 점에서 1번 목적과 정확히 맞물린다

**선택·입력 과정** — 색 선택까지의 시간 / **색을 바꾼 횟수**(자기 규정의 흔들림) /
가명·명명의 **입력 소요·수정 횟수·지운 횟수**(언어를 찾기 어려웠는가) /
**각 박자별 소요 시간**(★ 미워한 면 vs 그 다른 면 중 무엇이 더 어려운가 → `harder_beat`)

**캡처·기념품** — 캡처 시각·횟수 / **재캡처 횟수**(자기 표상에 대한 까다로움) / 결과 화면 체류 / 공유 실행 여부

**여정·동선** — 화면 전환 순서 전체 / 모듈 진입 시각·순서·**재진입 횟수** /
모듈 간 이동 시간 / 총 체류(단일 값·대조군) / 중도 이탈 지점 / 기기 종류(익명)

### 7-2. TD에서 나오는 것 — 가장 풍부한 층

> TD는 이미 초당 수십 건의 상태를 안다. 문제는 "무엇을 보낼지" 고르는 것뿐.
> 🟢 브리지는 완성. ⬜ **각 TD 프로젝트에 `push_event` 호출을 심는 작업이 남아 있다.**

**메인1 — 장미 정원 (귀속: 팀)**
채널별 터치 on/off 시각(16채널) / **홀드 지속 시간 분포**(탭=회피 vs 홀드=직면) /
첫 터치까지의 시간(진입 망설임) / 터치 간 간격(서두름 vs 음미) / **긍정·부정 채널 비율** /
**조합 패턴** / 조합을 바꾼 횟수 / **시간에 따른 극성 궤적** / 리셋 사용 /
**🌟 회로 끊김·재연결 이벤트 — 손을 놓은 순간 = 인간사슬이 끊긴 순간. 이 작품 고유의 관계 데이터** / 회로 유지 총 시간

**부1 — 집전 게임 (귀속: 팀 / 엔딩 저장은 개인)**
아이템 사용 순서·횟수 / **살림 vs 죽임 비율**(자기 대우 방식) / 리스폰 횟수 / 게임 총 시간 /
**엔딩 파라미터 값** / **재플레이 횟수**(엔딩이 미고지라 재플레이로만 발견) /
**🌟 재플레이 간 파라미터 변화 — 두 번째엔 다르게 대했는가**

**부2 — 애도 영상 (귀속: 개인 — 색 시그니처로 식별)**
오브젝트 접촉 순서 / 장면 전환 횟수 / **발견까지 걸린 시간** / 아바타 활성 시 반응 지연 /
**dwell 누적 총량 = 목격의 정량화(부2 고유 지표)** / 엔딩 도달 / 공용 화면의 타인 명명을 보던 시간

### 7-3. 태깅에서 나오는 것

모듈 진입 시각·순서·재진입 🟢 /
**결합 누락률**(TD 인터랙션은 있는데 태깅이 없는 비율) — **그 자체가 UX 지표** 🟢 (`v_attribution_gap`)

---

## 8. 파생 지표와 산출 방법

> raw만 잘 쌓으면 나중에 얼마든지 만들 수 있다. 지금 정할 필요 없음.

### 8-1. 사업 방향문서가 요구하는 "관계적 지표"와의 대응

방향문서 §6: 단일 값(체류시간·방문자수·클릭수)은 **피한다** — 누구나 셈·얕음·자명.
핵심은 여러 행동의 **관계·패턴·시퀀스**.

| 관계적 지표 | 이 작품에서의 구체 형태 | 설계에 있나 |
|---|---|---|
| **되돌아옴** | ①메인1 재방문 ②**부1 재플레이 — 엔딩이 미고지라 재플레이로만 발견** | ✅ 둘 다 |
| **여정에 따른 반응 변화** | ①**가명 → 최종 명명의 변화** ②메인1 최초 조합 → 재방문 조합의 극성 변화 ③부1 첫 플레이 → 재플레이 파라미터 변화 | ✅ |
| **수렴/발산** | ①명명 어휘 분포 ②조합 분포 ③헌화 위치 분포 | ✅ |
| **순서·경로 선택** | 부1↔부2 순서 자유 = 선택 자체가 데이터 | ✅ |

**→ 측정 설계를 새로 만드는 게 아니라, 이미 있는 것을 데이터로 꺼내는 문제.**

**⚠️ 단일 값도 반드시 수집한다 — 자산이 아니라 대조군으로.**
무너짐 조건: *관계적 지표와 단순 카운팅의 설문 예측력이 비슷하면 방향 재고.*
→ 비교 대상이 없으면 검증 자체가 성립 안 함.

### 8-2. 산출 (쿼리 수준) — 뷰로 미리 만들어뒀다

| 지표 | 산출 | 뷰 |
|---|---|---|
| 지금 몇 명 안에 | active 세션 수 | `v_live_count` |
| 되돌아옴 비율 | 같은 세션+스테이션에 `enter` 2회 이상 | `v_returning` |
| 가명→최종명명 변화 | 세션별 대조 + 길이 차 | `v_naming_shift` |
| 결합 누락률 | TD 인터랙션 중 세션 귀속이 없는 비율 | `v_attribution_gap` |
| 모듈별 체류 | `enter` ~ 다음 이벤트 시각 차 | 쿼리 |
| 총 체류(단일값·대조군) | 입장 ~ 설문 완료 | 쿼리 |
| 몰입도 | `phone_away_*` 합 − `rules_*` 구간 | 쿼리 |
| 망설임 프로파일 | 색 선택 시간 · 첫 터치 지연 · 명명 수정 횟수 | 쿼리 |

### 8-3. 🔴 아직 없는 것 (사업 검증)

1. **A/B 개입 설계** — 개입 그룹 vs 비개입 그룹 비교. `sessions.ab_group` 컬럼만 비워둔 상태.
   **이번 전시에서는 안 넣었다** — 설계가 백지인데 지금 넣으면 측정 때문에 작품을 바꾸게 된다.
   ⚠️ 규율: **효과가 먼저, 데이터는 도구. 측정 때문에 작품을 바꾸지 않는다.**
2. **설문의 검증 도구화** — 관계적 지표가 설문 반응을 예측하는지 보려면
   설문이 "경험의 질"을 재는 문항이어야 함. **초안(척도 4문항)이 이 목적을 겸하게 설계됐다.** 컨펌 대상.
3. **일반화 검증** — 이 데이터가 남의 전시에도 적용될 만큼 일반적인가.
   **스키마를 작품 비의존적으로 유지하는 것이 곧 이 검증.**

---

## 9. 인프라·배포

### 확정 구조 (8/10)

```
관객 폰  →  GitHub Pages (정적·HTTPS 자동)  →  Supabase (DB)
TD       →  Supabase REST + 로컬 CSV (동시 기록)
폰 웹앱  →  오프라인 큐잉 (끊기면 브라우저 보관 → 복구 시 전송)
```

**⚠️ "로컬 서버를 주로 쓰는" 안은 기각됐다 — HTTPS 함정.**
전시장 노트북에 서버를 띄우고 관객 폰이 `http://192.168...`로 접속하면
**브라우저가 카메라·마이크·공유 API를 차단**(secure context 아님) → **QR 스캔 자체가 불가.**
HTTPS 페이지에서 로컬 http로 연결하는 것도 혼합 콘텐츠로 차단. 관객 폰마다 인증서 설치도 불가능.
**같은 와이파이를 써도 이 제약은 그대로.** → GitHub Pages가 이걸 자동으로 푼다.

**⚠️ URL 형식 변경 (8/10):** `/enter?station=NN` → **`/?station=NN`**
GitHub Pages는 정적이라 `/enter` 경로를 만들 수 없다. QR·NFC 모두 같은 URL이라는 원칙은 그대로.

**⚠️ B1 지하 = 네트워크 불안정 리스크.** 로컬 CSV 이중 기록은 선택이 아니라 필수.
**3일치를 한 번의 장애로 잃으면 검증 자체가 무너진다.**

### Supabase 무료 구간 (2026)

DB 500MB · 파일 1GB · 동시 실시간 연결 200 · 프로젝트 2개

- 텍스트·이벤트는 3일 700명 해도 수 MB, 동시 접속 최대 20명 → 여유
- **⚠️ 캡처 이미지가 초과 요인:** 700명 × 4장 × 500KB ≈ 1.4GB > 1GB
  → **현재 코드는 ①이미지는 TD 로컬 보관·DB엔 경로만** 전제로 짜여 있다. ②유료 전환($25)은 대안
- **오픈소스라 락인 없음** — 기관 온프렘 요구 시 자체 호스팅으로 이전 가능

### 🔴 순서가 중요하다

```
GitHub Pages 켜기  →  URL 확정  →  QR 생성  →  인쇄 발주 + NFC 쓰기
                                    (Supabase는 이 뒤에 해도 됨)
```

**Supabase 키가 비어 있어도 앱은 전부 작동한다.** 로컬 모드로 쌓아두고 키를 넣는 순간 밀린 것이 올라간다.
→ **URL 확정을 먼저 하면 인쇄 일정이 안 막힌다.** 명령어는 `README.md §1~3`.

---

## 10. TouchDesigner 연동

`td_bridge.py`를 Text DAT에 붙여넣고 이름을 `td_bridge`로. 상단 설정 4개만 채운다.

### 설계 원칙 — S11-D~H의 교훈을 그대로 이식했다

1. **통신은 Worker Thread에서.** TD를 절대 멈추지 않는다
2. **전송 실패가 캡처·게임·렌더에 영향을 주지 않는다** (S11-D 완전 분리 원칙)
3. **★ 활성 세션 판별은 `status == 'active'`** — 서버가 종료 후에도 마지막 metadata를 유지하므로
   `session_id`가 비었는지로 판단하면 안 된다 (S11-F)
4. 🔴 **세션이 없으면 색·이름을 반드시 비운다** — 안 비우면 **이전 관객의 색이 다음 사람에게 적용된다.**
   실측 테스트로 고정돼 있다
5. **재연결 순간 backoff를 무시하고 즉시 재전송** (S11-H `upload_reconnect_wake`)
6. 🔴 **로컬 CSV 동시 기록** — `~/fringe2026_logs/`. 전송이 실패해도 CSV엔 남는다
7. 의존성 없음 — Python 표준 라이브러리만. (TD Web Client DAT의 `formParts`·`mimeParts` 미지원을 우회한 것과 같은 이유)

### 최소 사용법

```python
# Execute DAT · onStart
mod('td_bridge').start()

# 🔴 세션 게이트 — 촬영·기록 전에 반드시 (S11-H 3단 게이트와 같은 자리)
a = mod('td_bridge').active()
if a['status'] != 'active' or not a['session_id']:
    return                      # 조용히 무시. 에러 아님.

# 인터랙션
mod('td_bridge').push_event('rose_touch',
    {'channel': 7, 'hold_ms': 3200, 'polarity': 'negative'}, scope='team')

# 세션 색
r, g, b = mod('td_bridge').session_color()

# 🔴 부1 엔딩 = 세션 종료 (촬영 여부와 무관)
mod('td_bridge').end_session_by_ending()
```

모듈별 전체 예시는 `td_bridge.py` 맨 아래 주석.

> **⚠️ 캡처 트리거 방향이 모듈마다 반대다.**
> 메인1 = **폰이 요청**(폰→서버→TD) / 부1 = **TD가 시작점**(윙크→TD→서버).
> 문서엔 오래 "A7과 동일 파이프라인"으로만 돼 있었다. 코드는 이 차이를 반영하고 있다.

---

## 11. QR·NFC 실물 사양

### 11-1. 왜 둘 다인가 — 서로의 구멍을 메운다

| | QR | NFC |
|---|---|---|
| 필요한 것 | 카메라 켜기 → 조준 → 초점 | **갖다 대기 한 번** |
| **어두운 곳** | ❌ **약함.** 카메라가 빛을 필요로 함 | ⭕ **무관** |
| 기종 | ⭕ **전부** | △ 기종·설정 탐 |
| 손 | 두 손이 될 때가 있음 | 한 손 |

> 🔴 **이 전시는 B1이고 장례식 톤이라 어둡다.** QR만이면 매 모듈 진입에서 조준·초점 실랑이가 생긴다.
> **NFC가 그 마찰을 없애고, QR이 기종 문제를 없앤다.**
> 기능상 먼저 시도되는 건 NFC, **리스크상 절대 못 빼는 건 QR**(인쇄물에 물림).

### 11-2. NFC — 정확히 뭘 사는가

| | |
|---|---|
| **칩** | **NTAG213** (13.56MHz · ISO14443A · NFC Forum Type 2) |
| **형태** | 원형 스티커 25mm |
| **조건** | **재기록 가능(rewritable)** — 공테그 |
| **수량** | **30장** (스테이션 6곳 + 예비·훼손분) — 🟢 8/10 구매 완료 |

**NTAG215·216은 불필요.** URL 하나는 35바이트 남짓, NTAG213의 144바이트로 충분.

**❌ 사면 안 되는 것 — 이거 하나만 틀려도 아이폰에서 통째로 안 읽힌다**

- **MIFARE Classic / S50 / "1K"** → iOS 미지원. 아이폰 전량 실패
- **RC522 키트에 딸려오는 흰 카드·키홀더** → 그게 정확히 MIFARE Classic이다. 이미 갖고 있어도 못 쓴다
- "읽기 전용" · 이미 URL이 구워진 판촉용 태그

> **리스팅에 `NTAG213`이라고 칩 이름이 박힌 것만.** 디바이스마트는 국내 재고 없음(Adafruit 해외배송 2주, 1장 5,400원)
> → **쿠팡 로켓배송 / 네이버 오늘출발**만 유효.

### 11-3. 기종 호환성 — 실측 확인 완료

| 기종 | 태그 읽기 |
|---|---|
| iPhone XR·XS 이상, **SE 2세대 이상** | ⭕ **앱 없이 자동** |
| iPhone **7·8·X** | △ **제어센터에서 "NFC 태그 리더"를 켜야** 읽음 |
| iPhone **6s 이하** | ❌ **불가** (Apple Pay 전용 하드웨어) |
| Android | ⭕ 대부분. 단 **설정에서 NFC가 켜져 있어야** · 일부 저가 모델은 미탑재 |

**기종과 무관하게 막히는 것**

- **화면이 꺼져 있거나 잠겨 있으면 안 읽힌다**
- **두꺼운·금속·카드지갑 케이스** → 감도 급락 또는 차단
- ⚠️ **안테나 위치를 관객이 모른다** — 아이폰 **뒷면 위쪽**, 안드로이드 **뒷면 중앙**.
  실패의 상당수가 기종 문제가 아니라 **엉뚱한 데를 갖다 대서**다

**대응 — 인쇄물·인디스트 스크립트 요구사항**

1. 안내판에 **"폰 뒷면 윗부분을 여기에"** + 손 그림. NFC 위치를 눈에 보이게
2. **QR을 바로 옆에 같은 크기로.** "안 되면 QR" 한 줄
3. 인디스트 멘트: *"NFC가 안 되면 그냥 QR 찍으시면 됩니다"* — 관객이 자기 폰을 탓하지 않게

### 11-4. 스테이션 목록 【⬜ 번호 확정 필요】

| ID | 스테이션 | 안내판 | NFC |
|---|---|---|---|
| `00` | **입장** — 세션 발급 | ✅ | ⬜ (QR만으로도 됨) |
| `01` | **메인1** — 명명 | ✅ | ✅ |
| `02` | **부1** — 재연 | ✅ | ✅ |
| `03` | **부2** — 애도 | ✅ | ✅ |
| `04` | **기록** — 제작과정 영상·헤드폰 | ✅ | ✅ |
| `05` | **출구** — 설문 | ✅ | ✅ |

> 🔴 `config.js`의 `STATIONS`와 `make_qr.py`의 `STATIONS`가 **같아야 한다.** 바꾸면 둘 다 바꿀 것.

### 11-5. 안내판

**크기: A4 세로 (210×297mm).** 집에서 출력 가능 · 폼보드 배접 쉬움 · 어두운 데서 읽히는 최소 크기.

```
┌─────────────────────────┐
│   02                    │  ← 스테이션 번호 (크게, 옅게)
│   부1 — 재연            │  ← 이름
│   ─────────────         │
│   한 줄 문장            │  ← ⚠️ 톤 주의
│        ◯                │  ← NFC 원 마크 (지름 40mm)
│   폰 뒷면 위쪽을 여기에  │     + 손 그림
│      ▓▓▓▓▓▓             │
│      ▓▓▓▓▓▓             │  ← QR  50×50mm
│      ▓▓▓▓▓▓             │
│  안 되면 QR을 찍어주세요 │  ← 작게
└─────────────────────────┘
```

**QR 규격 — 어두운 공간이라 여기가 실무 급소**

| | |
|---|---|
| 크기 | **50×50mm.** 스캔 거리 ≈ 한 변 × 10 → 50mm면 **50cm**에서 잡힘 |
| 여백 | 둘레에 **최소 4모듈 분량의 흰 여백**(quiet zone). 없으면 인식률 급락 |
| 색 | **흰 바탕 · 검정 코드.** 색 반전·컬러 금지 |
| 용지 | 🔴 **무광.** 유광·코팅은 조명 반사로 QR도 글씨도 다 죽는다 |
| 인쇄 | **레이저.** 잉크젯은 B1 8월 습기에 번진다 |
| 오류정정 | **레벨 H** — 어두움 + 3일 700명 훼손 전제 (`make_qr.py`가 자동 적용) |

> 🔴 **안내판마다 국소 조명(작은 클립등·LED 스트립)이 필요한지 8/14 현장에서 반드시 실측.**
> 이게 NFC를 병기하는 가장 실질적인 이유다 — NFC는 빛이 필요 없다.

### 11-6. 거치 — ⚠️ 금속이 NFC를 죽인다

```
❌ 금속 이젤 · 금속 스탠드 · 삼각대에 안내판 직접 부착
❌ 금속 프레임 테이블 위에 안내판 눕히기
❌ 해골·금속 오브젝트에 스티커 직접 부착
```

금속에 유도되는 전류가 NFC 자기장을 상쇄해 **아예 안 읽힌다.**
**해결:** 비금속 거치(나무·아크릴·폼보드 자립) / 또는 금속 면과 안내판 사이에 **최소 5mm 공기층**

- NFC 스티커는 **안내판 앞면**에 (종이·폼보드는 전파가 통과하지만 **앞면이라야 관객이 어디 댈지 안다**)
- 안내판 **높이: 바닥에서 110~130cm**
- 확보된 **흰 테이블 5개 + 폼보드 자립 스탠드**가 가장 싸고 빠름
- ⚠️ 3일·700명이라 **넘어짐·훼손을 전제** → 여분 1세트

### 11-7. NFC 쓰기

`NFC Tools` 앱(무료·iOS/Android) → **Write → Add a record → URL**
→ `make_qr.py`가 만든 `qr/_NFC_URL목록.txt`의 주소를 그대로 입력

- **URL 최종 확정 후 읽기전용 잠금(lock)** — 관객 폰이 덮어쓰는 사고 방지
- ⚠️ **잠그면 되돌릴 수 없다.** 잠그기 전 다른 폰 2대로 읽기 테스트

### 11-8. 문구 톤 — 금지 사항

> **"체험해보세요" · "즐겨보세요" · "재미있게" 류 금지.** 장례식 톤이 깨진다.

| ❌ | ⭕ |
|---|---|
| "재미있게 체험해보세요!" | (조용한 서술 · 지시가 아니라 안내) |
| "지금 시작하기 👆" | "여기서 시작합니다" |

**문구는 작가 영역.** 안내판 문구와 `content.js`의 폰 문구를 **같이 쓸 것** — 같은 목소리여야 한다.

---

## 12. 폴백 — 폰이 죽어도 전시는 성립한다

| 상황 | 대응 | 상태 |
|---|---|---|
| NFC 안 됨 | QR — 안내판에 같은 크기로 나란히 | 🟢 |
| QR도 안 읽힘 | 대기 화면 하단 **"작품 표시가 안 읽히면 여기"** → 번호 직접 선택 | 🟢 |
| 네트워크 끊김 | 화면은 그대로 진행. 브라우저에 쌓였다가 복구 시 자동 전송 | 🟢 |
| Supabase 미설정 | 로컬 전용 모드로 작동. 키를 넣으면 밀린 것이 올라감 | 🟢 |
| 브라우저 닫음·배터리 | 세션·색·진행상태가 localStorage에 남아 복구됨 | 🟢 |
| 세션 미완료 | 회차 종료 시각으로 자동 마감 (`close_stale_sessions`) | 🟢 |
| TD 크래시 | 로컬 CSV는 append라 이전 기록 보존 | 🟢 |
| 폰이 통째로 안 됨 | **물리 인터랙션이 본체다.** 스태프가 수동 안내 | — |

**스태프용**

- `?reset=1` — 세션 초기화(다음 관객). 🔴 **큐는 안 지운다** — 아직 안 올라간 이전 관객 데이터가 들어 있다
- `config.js`의 `DEBUG: true` — 화면 하단에 이벤트 실시간 표시

---

## 13. 회수·백업

**전시 중 실시간 조회 가능** — 종료까지 기다릴 필요 없음(운영 모니터링 겸용).

```sql
select * from v_live_count;          -- 지금 몇 명 안에
select * from v_returning;           -- 되돌아옴
select * from v_naming_shift;        -- 가명 → 최종 명명의 변화
select * from v_attribution_gap;     -- 결합 누락률 (그 자체가 UX 지표)
select close_stale_sessions(90);     -- 회차 종료 자동 마감
```

| 방법 | 용도 |
|---|---|
| Supabase 대시보드 → 테이블 CSV 다운로드 | 가장 간단. 전시 후 CSV 6개 |
| SQL 쿼리 직접 실행 | 관계적 지표 대부분 쿼리 한 줄 |
| API 호출 | 리포트 자동화(사업화 단계) |

**🔴 매일 전시 종료 직후 백업.** 3일치를 한 번의 장애로 잃으면 검증 자체가 무너진다.
**회차 종료마다 대조** — TD의 `queue_size()`, 로컬 CSV 행수, Supabase 행수를 비교하면 유실을 그 자리에서 발견한다.

### 프라이버시

- **개인 식별보다 익명화·집계 중심.** 세션ID는 있으나 신원 정보 없음(계정 불필요 구조와 정합)
- 익명 세션 · **얼굴 원본 미저장**
- **동의 절차** — 첫 화면에 배치, 보관 기간·삭제 방법 포함. 🟡 문안 초안, ⬜ 삭제 요청 연락처 비어 있음
- 익명 자동감지 데이터는 개인과 결합 불가 → 별도 테이블(`anon_presence`)

---

## 14. 🔴 컨펌 대기

| # | 항목 | 어디를 고치나 | 왜 급한가 |
|---|---|---|---|
| 1 | **GitHub 계정·저장소 이름** | — | 🔴🔴 **URL이 여기서 나오고 인쇄가 물려 있다. 오늘 안에** |
| 2 | **색 팔레트 10색** | `config.js` PALETTE | 🔴 부2 아바타 시그니처. **마스킹 클립 3~4개 띄워놓고 눈으로 대조** — 배경에 잘 없는 색이어야 아바타가 튄다 |
| 3 | **큐레이토리얼 문구 전체** | `content.js` | 전부 초안. 문구는 작가 영역. 안내판 문구와 같이 쓸 것 |
| 4 | **설문 문항** | `content.js` survey | 초안 = 자유서술 1(필수) + 척도 4 + 선택 1. 검증 도구 겸용으로 설계 |
| 5 | **동의 문구·보관기간·삭제 연락처** | `content.js` consent | ⬜**이메일주소** 자리가 비어 있음. 보관 2년은 초안 |
| 6 | **스테이션 번호 확정** | `config.js` + `make_qr.py` | 둘이 같아야 함 |
| 7 | **인스타 계정 URL** | `config.js` | 출구 화면 |
| 8 | **캡처 이미지 저장 방식** | — | 로컬 보관(현재 전제) vs 유료 전환 |
| 9 | **A/B 개입 지점** | — | 이번엔 안 넣음. 넣을지 여부 |
| 10 | **색 이름 노출 여부** | `config.js` SHOW_COLOR_NAMES | 기본 숨김. 보여주면 시스템이 감정 어휘를 먼저 주는 셈이라 논문 원칙과 충돌 소지 |

---

## 15. 이번에 뺀 것 · 추후 논의

### 이번 전시 컷 라인

말 남기기(A10) · LLM 되비춤 · 익명 재실 통계(C8) · **자동감지 전체** · A/B 개입 설계

**자동감지를 뺀 근거:** 태깅이 사주는 건 **"진입 시각·체류시간의 정밀도"**이지 데이터의 존재 자체가 아니다.
**작품이 이미 각 모듈에서 폰 행동을 요구**하므로(메인1 캡처·명명 / 부1 엔딩 / 부2 스샷)
방문 여부·순서는 태깅 없이도 산출된다. 체류시간은 **"단일 값(피함)"** 범주라 고가 하드웨어는 과투자.
(기술 비교 전문은 `20_자동감지_기술비교.md`)

### 전시 후 · 사업 단계

| 항목 | 내용 |
|---|---|
| **데이터 가공 3종 설계** | 기관용 결과보고 / 관객용 기념·되비춤 / 아티스트용 개선 인사이트 |
| **대표 관계적 지표 선정** | 작품 분석 후 결정 |
| **무너짐 조건 검증** | 관계적 지표 vs 단순 카운팅의 설문 예측력 비교 |
| **비인터랙티브 작품용 모드** | 폰 허브가 "작품과 교류하게 하는 인터랙티브 모듈"이 되는 형태 |
| **자체 감지 키트 개발** | ESP32 베이스 + 센서 모듈 |
| 온프렘(기관 서버) 이전 | Supabase 오픈소스라 가능 |
| 다전시 멀티테넌시 · 스키마 버전 관리 | 두 번째 전시 적용 시 |

### 파네시마·멜번 이월

| 항목 | 내용 |
|---|---|
| **자동감지 본격 적용** | 파네시마는 무인 2주 → 탭 유도가 구조적으로 불가. 실익이 프린지보다 큼 |
| 무인 운영 시 세션 발급·종료 | 스태프 없이 어떻게 |
| 초음파 재검토 | 조용한 전시장이면 가능. 네이티브 앱 도입 여부와 연동 |
| 프린지 데이터의 재사용 | 전시 간 비교가 가능한 스키마 유지 |

---

## 16. 파일 목록

| 파일 | 내용 | 작가가 고칠 일 |
|---|---|---|
| **`폰허브_MASTER.md`** | **이 문서 — 폰 허브에 관한 단일 진입점** | 결정할 때마다 |
| `README.md` | 배포 실무 절차(명령어). GitHub에 함께 올라감 | ✕ |
| `index.html` | 껍데기 | ✕ |
| `styles.css` | 다크 톤·큰 터치 타깃 | 톤 조정 시 |
| **`config.js`** | 키·스테이션·**색 팔레트** | ⭕ |
| **`content.js`** | **모든 문구** | ⭕⭕ |
| `db.js` | 세션·이벤트·오프라인 큐 | ✕ |
| `measure.js` | 읽기·미사용·입력과정 측정 | ✕ |
| `app.js` | 화면 흐름 | ✕ |
| `supabase_schema.sql` | DB 스키마 + 뷰 + 폴백 함수 | ✕ |
| `td_bridge.py` | TD 연동 | 상단 설정 4줄 |
| `make_qr.py` | QR 일괄 생성 | ✕ |

### 원본 문서 (경위·근거가 필요할 때만)

| 문서 | 여기에 흡수된 부분 |
|---|---|
| `../21_폰허브_핸드오버.md` | 전체 명세 · 3층 관계 · 기능 · 화면흐름 · 귀속 · 인프라 · 태깅 |
| `../22_데이터측정_설계.md` | 목적 계층 · raw data 전수 · 파생 지표 · harvest 원칙 · 프라이버시 |
| `../30_안내판_NFC사인_사양.md` | QR·NFC 실물 사양 전체 (§11) |
| `../11_디벨롭로그.md` | 의사결정 연대기(번복 포함). **여긴 계속 쓴다** |
| `../20_자동감지_기술비교.md` | 자동감지 기술 전면 비교·가격·출처 (이번엔 미채택) |
| `../23_부1_게임설계.md` | 부1 규칙 — B5 안내 문구의 출처 |
| `../25_부2_설계.md` | 부2 배정·발견 규칙 — 색 팔레트 요건의 출처 |
