# META ROSE Phone Hub — 현행 결정과 이전 핸드오버 문서의 차이

작성일: 2026-08-12  
적용 범위: META ROSE 2026 Phone Hub, Supabase, TouchDesigner 연동

## 이 문서의 역할

`21_폰허브_핸드오버.md`와 `22_데이터측정_설계.md`는 프로젝트 초기에 만든 설계 기록이다. 두 문서는 삭제하거나 무시할 자료가 아니라, 왜 그런 판단을 했는지를 보존하는 참고 자료다.

다만 아래 항목은 이후 확정된 방향과 다르다. 구현과 전시 운영에서는 **이 문서와 현재 웹 코드의 방향을 우선**한다. 화면 문구는 별도 카피 승인 문서의 최신 확정본을 따른다.

## 그대로 유지되는 핵심

- QR/NFC는 Phone Hub URL과 station id를 전달한다.
- Supabase는 전시 세션, station 방문, 이벤트, artifact, 설문을 보관하는 중앙 저장소다.
- 관객의 실명, 연락처, 계정 가입은 받지 않는다.
- 브라우저에는 오프라인 큐가 있어 네트워크가 잠시 끊겨도 기록을 보관하고, 다음 가능 시점에 전송한다.
- TouchDesigner의 원시 인터랙션은 우선 각 TD 컴퓨터의 로컬 CSV에 남긴다. 네트워크 전송이 작품 프레임을 멈추게 해서는 안 된다.
- 이벤트에는 `session_id`, 발생 시각, station id, event type, payload가 남아 후에 세션 단위로 분석할 수 있다.

## 달라진 결정

### 1. 시작 단계와 이름

이전 문서에는 임시 별칭(pseudonym)을 먼저 입력하는 흐름이 남아 있다.

현재 흐름은 다음이다.

```text
Arrival → 나의 장미 색 선택 → HOME → 01 명명 → 감정명명 → 02 / 03 / 04 → Exit
```

- 처음에는 본명·별칭을 입력하지 않는다.
- 장미 색과 무작위 장미 번호가 관객의 전시 중 식별자다.
- 감정명은 MAIN1 뒤에 권장·필수 흐름으로 만든다.
- 기존 DB의 `pseudonym`, `final_name` 계열 컬럼은 급한 migration을 피하기 위한 호환용 컬럼일 뿐, UI에서 개인정보를 받는다는 뜻은 아니다.

### 2. 익명성 및 관객 간 분리

이전 문서의 단순 익명 세션보다 한 단계 강화했다.

- 최초 입장 시 Supabase Anonymous Auth로 브라우저 전용 익명 `auth.uid`를 발급한다.
- RLS 정책이 그 uid의 세션·이벤트·설문만 읽고 쓰도록 제한한다.
- 한 관객은 다른 관객의 세션, 이름, 결과, 이미지, 행동 데이터를 읽을 수 없다.
- 전시 종료 뒤 Phone Hub 화면은 새 세션처럼 시작할 수 있지만, 내부 Supabase에는 익명 세션 단위의 기록이 남는다.

### 3. TD와 Phone Hub의 연결 방식

이전 문서에는 TD의 원시 이벤트를 시간 매칭하거나 Phone으로 실시간 전달하는 구상이 일부 있다. 이는 전시 안정성 원칙에 맞지 않아 바꾸었다.

```text
TD 원시 인터랙션
  → TD 내부 계산 + 로컬 CSV
  → 필요한 checkpoint / summary만 Supabase
  → Phone Hub는 해당 결과 화면에 들어올 때 1회 읽기
```

- Phone Hub는 TD 실시간 대시보드가 아니다.
- TD는 Phone과 WebSocket으로 직접 통신하지 않는다.
- Phone은 결과 화면에서만 자기 세션의 요약 artifact를 읽는다. 지속 polling은 하지 않는다.
- MAIN1 / SUB1 / SUB2는 각각 raw interaction 수십 개가 아니라 summary trace 하나를 최종 전달한다.

| 작품 | 현행 summary | 상태 |
| --- | --- | --- |
| 01 명명 / NAMING | `resonance_trace` | P0 데이터 계약 확정, TD 계산 규칙은 MAIN1에 연결 중 |
| 02 개입 / INTERVENTION | `mutation_trace` | P0 계약 확정, SUB1 이식 후 연결 |
| 03 목격 / WITNESS | `temporal_trace` | P0 계약 확정, SUB2 연결 예정 |
| 04 기록 / RECORD | 점수 없음 또는 `record_trace` | P1 — 체류·태깅만 우선 |

MVP 값은 `0 / 1 / 2` 또는 `low / mid / high`면 충분하다. Phone은 그 계산식을 알 필요가 없다.

### 4. 이미지 및 캡처

- TD가 만든 스크린샷·영상의 원본 파일은 TD 로컬 디스크에 보관한다.
- Phone Hub에 즉시 원본 이미지나 영상을 업로드하지 않는다.
- Supabase에는 필요한 경우 artifact의 타입, 생성 시각, 로컬 파일 식별자·경로 메타데이터, 요약만 쓴다.
- Storage 업로드와 Phone에서 실제 이미지를 보여주는 기능은 **P1**이다. 전시 안정성 검증 뒤에만 연다.

이 방식은 무료 Storage 용량·화질·전송 대기 문제와 개인정보 위험을 전시 본선에서 분리한다.

### 5. 세션 종료 시점

이전 초안의 “SUB1 결과 촬영 후 세션 종료”는 폐기했다.

- 세션 종료는 Exit QR/NFC(`station=05`) → Final Reflection 완료 시점이다.
- 미방문 작품이 있으면 돌아갈지, 현재 기록으로 완성할지 선택한다.
- 마지막 설문과 자유 소감, 최종 specimen을 저장한 뒤 `sessions.is_final`을 true로 만든다.

### 6. 행동 데이터의 전송 방식

관객 경험에 즉시 필요한 데이터는 바로 저장한다.

- station enter / leave
- 감정명 저장
- TD checkpoint / trace summary
- Exit 및 설문 완료

그 외 화면 클릭, 스크롤·읽기 체류, 상세 설명 열기, 색상 변경 횟수 등의 Phone Hub 분석 이벤트는 브라우저 큐에 모았다가 다음 안전한 이동 지점에 묶어 보낸다.

```text
화면 전환 · station 이동 · 탭이 백그라운드로 갈 때 · Exit · 네트워크 회복 시
```

따라서 전시 중 조작감은 가볍게 유지하면서도, 수집 가능한 Phone 행동 기록은 세션별로 `events`에 남는다. 브라우저가 강제로 종료되거나 배터리가 꺼지면 아직 전송되지 않은 마지막 큐 일부는 손실될 수 있다. 이 위험은 완전히 없앨 수 없으므로, 중요한 기록은 즉시 저장하고 나머지는 중간 checkpoint를 여러 번 둔다.

### 7. 설문

이전 문서의 짧은 최소 설문 대신, 현재 웹은 감정 경험·Phone Hub 기여·상호작용 명료성을 구분해서 읽을 수 있는 **10개 1–10 척도 + 선택 자유 소감** 구조다.

- 항목 후보 100개와 분석 근거는 `FINAL_REFLECTION_SURVEY_BANK_100_V1.md`에 보관한다.
- 현재 전시용 10문항은 `FINAL_REFLECTION_SURVEY_CANDIDATES_V1.md` 및 웹 코드에 있다.
- 문항은 최종 컨펌 전이므로 조정 가능하다.

### 8. 이번 전시에서 의도적으로 보류한 것

다음은 기능이 부족해서가 아니라 3일 전시의 안정성·익명성·운영 난이도를 고려해 P1/P2로 둔 항목이다.

- 관객 팀·round·동반 관객 매칭
- 실시간 혼잡도·대기열·다른 관객의 상태 표시
- 카메라·센서를 이용한 자동 위치 추정과 자동 태깅
- Phone에서 TD 원시 데이터나 라이브 프레임을 보여주기
- 사진·영상 원본의 Supabase Storage 업로드 및 공유
- 관객 간 메시지·공개 피드·결과 비교
- LLM 해석, 추천, 개인화 피드백
- 실시간 관리자 대시보드

## 운영상 최종 원칙

1. 작품이 먼저다. 네트워크 오류가 TD 인터랙션을 멈추면 안 된다.  
2. Phone Hub는 세션의 등뼈와 결과 UI다. TD 화면의 복제본이 아니다.  
3. 관객에게 숫자 점수나 타인의 데이터를 보여주지 않는다. 장미의 변화와 자기 기록만 보여준다.  
4. 모든 데이터는 익명 세션 단위이며, 공개·배포하지 않는 내부 연구·개선용 데이터다.  
5. P0가 안정적으로 작동한 뒤에만 이미지·영상·자동화 같은 무거운 기능을 연다.

